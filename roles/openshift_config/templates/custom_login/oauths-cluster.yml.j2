apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  templates:
    error:
      name: error-template
    login:
      name: login-template
    providerSelection:
      name: providers-template
